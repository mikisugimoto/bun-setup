---
all:
    children:
        linux:
            children:
                local:
                    hosts:
                        localhost:
                    vars:
                        ansible_connection=local
